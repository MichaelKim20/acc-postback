<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="table">
    <sql id="postback">
        CREATE TABLE IF NOT EXISTS items
        (
            "postback_id" VARCHAR(64) NOT NULL,
            "event_name" VARCHAR(64) NOT NULL,
            "provider" VARCHAR(42) NOT NULL,
            "user_id" VARCHAR(42) NOT NULL,
            "user_id_type" INTEGER NOT NULL,
            "payout" NUMERIC(10,4) NOT NULL,
            "user_payout" NUMERIC(10,4) NOT NULL,
            "user_payout_in_vc" NUMERIC(10,4) NOT NULL,
            "status" VARCHAR(12) NOT NULL,
            "timestamp" TIMESTAMP,
            "tx_hash" VARCHAR(66) DEFAULT '',
            PRIMARY KEY("postback_id")
        );
    </sql>

    <select id="create_table">
        <include refid="postback"/>
    </select>

    <select id="drop_table">
        DROP TABLE items;
    </select>
</mapper>
